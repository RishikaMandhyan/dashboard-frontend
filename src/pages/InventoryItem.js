import Layout from "./Layout";
import IItem from "../components/InventoryItem";

const InventoryItem = () => {
  return (
    <Layout>
      <IItem />
    </Layout>
  );
};

export default InventoryItem;
